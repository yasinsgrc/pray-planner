node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
/data/
.visual/

# Ajan araç artıkları — projenin parçası değil (design-refresh-v3 Faz 9 F3).
.agents/
.claude/
skills-lock.json

# Local Netlify folder
.netlify

# Android (design-refresh-v3 Faz 23 Commit 1) — build output and
# machine/keystore-specific files never belong in version control. Signing
# stays entirely with the developer: keystore.properties.example is the
# only committed template, the real file (and the keystore itself) never is.
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/keystore.properties
*.keystore
*.jks

# OSM ham veri önbelleği (scripts/places/fetch-osm.ts) — üretilen veri, versiyonlanmaz.
scripts/places/.cache/

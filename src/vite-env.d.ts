/// <reference types="vite/client" />

/** vite.config.ts'in `define`'ı ile package.json'dan enjekte edilir — geri bildirim mailto'sundaki tanı bilgisi için. */
declare const __APP_VERSION__: string;
